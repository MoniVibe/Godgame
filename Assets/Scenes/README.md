This folder is intentionally tracked so the registry SubScene wizard can save assets here.

Use `Tools ▸ Godgame ▸ Create Registry SubScene…` in the Unity Editor to generate `GodgameRegistrySubScene.unity` with PureDOTS runtime config, spatial profile, and sample registry authoring.
