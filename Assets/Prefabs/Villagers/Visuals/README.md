# Villager Visual Prefabs

`VillagerView.prefab` is a straight copy of `TT_Peasant` from the Toony Tiny RTS pack so we can
change materials or overrides without touching the original asset pack. Point
`StaticVisualAuthoring` at prefabs in this folder when you need different outfits per role.
